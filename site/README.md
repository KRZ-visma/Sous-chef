# Sous-chef web app

Vite + React + TypeScript. Recipe JSON lives in `public/data/recipes/` and is fetched at runtime.

```bash
npm install
npm run dev
```

Production output is written to `dist/` (`npm run build`). GitHub Actions publishes `dist/` to Pages.
